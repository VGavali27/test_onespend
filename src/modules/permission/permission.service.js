import permissionRepository from './permission.repository.js';import ApiError from '../../utils/ApiError.js';class PermissionService {  // Fetch all permissions  async getAll() {    return permissionRepository.findAll();  }  // Fetch a single permission by UUID — throws 404 if missing  async getByUuid(uuid) {    const permission = await permissionRepository.findByUuid(uuid);    if (!permission) {      throw ApiError.notFound('Permission not found');    }    return permission;  }  // Create a new permission — checks key uniqueness  async create(data) {    if (data.permission_key) {      const existing = await permissionRepository.findByKey(data.permission_key);      if (existing) {        throw ApiError.conflict('Permission key already exists');      }    }    return permissionRepository.create(data);  }  // Update a permission by UUID  async update(uuid, data) {    const permission = await permissionRepository.findByUuid(uuid);    if (!permission) {      throw ApiError.notFound('Permission not found');    }    if (data.permission_key && data.permission_key !== permission.permission_key) {      const existing = await permissionRepository.findByKey(data.permission_key);      if (existing) {        throw ApiError.conflict('Permission key already exists');      }    }    return permission.update(data);  }  // Soft delete a permission by UUID — throws 404 if missing  async delete(uuid) {    const deleted = await permissionRepository.delete(uuid);    if (!deleted) {      throw ApiError.notFound('Permission not found');    }    return { message: 'Permission deleted successfully' };  }}export default new PermissionService();