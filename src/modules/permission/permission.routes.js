import { Router } from 'express';import * as permissionController from './permission.controller.js';import validate from '../../middleware/validate.js';import { createPermissionSchema, updatePermissionSchema } from './permission.validation.js';const router = Router();// List all permissionsrouter.get('/', permissionController.getAllPermissions);// Get a single permission by UUIDrouter.get('/:uuid', permissionController.getPermissionByUuid);// Create a new permission (validate body first)router.post('/', validate(createPermissionSchema), permissionController.createPermission);// Update an existing permission by UUID (validate body first)router.put('/:uuid', validate(updatePermissionSchema), permissionController.updatePermission);// Soft delete a permission by UUIDrouter.delete('/:uuid', permissionController.deletePermission);export default router;