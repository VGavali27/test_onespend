import Joi from 'joi';// Schema for creating a permissionexport const createPermissionSchema = Joi.object({  resource: Joi.string().required().messages({    'string.empty': 'Resource is required',  }),  action: Joi.string().required().messages({    'string.empty': 'Action is required',  }),  permission_key: Joi.string().required().messages({    'string.empty': 'Permission key is required',  }),  description: Joi.string().allow(null, ''),});// Schema for updating a permission — all fields optional, at least one requiredexport const updatePermissionSchema = Joi.object({  resource: Joi.string(),  action: Joi.string(),  permission_key: Joi.string(),  description: Joi.string().allow(null, ''),}).min(1).messages({ 'object.min': 'At least one field is required' });