import { Router } from 'express';import * as controller from './travel_accommodation.controller.js';import validate from '../../middleware/validate.js';import { createAccommodationSchema, updateAccommodationSchema } from './travel_accommodation.validation.js';const router = Router();router.get('/', controller.getAll);router.get('/:uuid', controller.getByUuid);router.post('/', validate(createAccommodationSchema), controller.create);router.put('/:uuid', validate(updateAccommodationSchema), controller.update);router.delete('/:uuid', controller.deleteRecord);export default router;