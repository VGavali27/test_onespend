import { Router } from 'express';import * as controller from './travel_segment.controller.js';import validate from '../../middleware/validate.js';import { createSegmentSchema, updateSegmentSchema } from './travel_segment.validation.js';const router = Router();router.get('/', controller.getAll);router.get('/:uuid', controller.getByUuid);router.post('/', validate(createSegmentSchema), controller.create);router.put('/:uuid', validate(updateSegmentSchema), controller.update);router.delete('/:uuid', controller.deleteRecord);export default router;