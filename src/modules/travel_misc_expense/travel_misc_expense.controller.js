import service from './travel_misc_expense.service.js';import ApiResponse from '../../utils/apiResponse.js';export const getAll = async (_r, res, n) => { try { const d = await service.getAll(); return ApiResponse.success(res, d, 'Misc expenses fetched'); } catch (e) { n(e); } };export const getByUuid = async (req, res, n) => { try { const d = await service.getByUuid(req.params.uuid); return ApiResponse.success(res, d, 'Misc expense fetched'); } catch (e) { n(e); } };export const create = async (req, res, n) => { try { const d = await service.create(req.body); return ApiResponse.created(res, d, 'Misc expense created'); } catch (e) { n(e); } };export const update = async (req, res, n) => { try { const d = await service.update(req.params.uuid, req.body); return ApiResponse.success(res, d, 'Misc expense updated'); } catch (e) { n(e); } };export const deleteRecord = async (req, res, n) => { try { const r = await service.delete(req.params.uuid); return ApiResponse.success(res, null, r.message); } catch (e) { n(e); } };