import { Router } from 'express';import * as expenseCategoryController from './expense_category.controller.js';import validate from '../../middleware/validate.js';import { createExpenseCategorySchema, updateExpenseCategorySchema } from './expense_category.validation.js';const router = Router();// List all expense categoriesrouter.get('/', expenseCategoryController.getAllCategories);// Get a single category by UUIDrouter.get('/:uuid', expenseCategoryController.getCategoryByUuid);// Create a new category (validate body first)router.post('/', validate(createExpenseCategorySchema), expenseCategoryController.createCategory);// Update an existing category by UUID (validate body first)router.put('/:uuid', validate(updateExpenseCategorySchema), expenseCategoryController.updateCategory);// Soft delete a category by UUIDrouter.delete('/:uuid', expenseCategoryController.deleteCategory);export default router;