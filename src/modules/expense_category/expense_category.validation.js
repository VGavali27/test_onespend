import Joi from 'joi';// Schema for creating an expense categoryexport const createExpenseCategorySchema = Joi.object({  code: Joi.string().max(50).required().messages({    'string.empty': 'Category code is required',    'string.max': 'Category code must be at most 50 characters',  }),  module: Joi.string().max(50).required().messages({    'string.empty': 'Module is required',    'string.max': 'Module must be at most 50 characters',  }),  name: Joi.string().max(100).required().messages({    'string.empty': 'Category name is required',    'string.max': 'Category name must be at most 100 characters',  }),  description: Joi.string().allow(null, ''),  first_receiver_role_uuid: Joi.string().uuid().required().messages({    'string.guid': 'First receiver role UUID must be a valid UUID',    'any.required': 'First receiver role UUID is required',  }),  final_approver_role_uuid: Joi.string().uuid().required().messages({    'string.guid': 'Final approver role UUID must be a valid UUID',    'any.required': 'Final approver role UUID is required',  }),});// Schema for updating an expense category — all fields optional, at least one requiredexport const updateExpenseCategorySchema = Joi.object({  code: Joi.string().max(50),  module: Joi.string().max(50),  name: Joi.string().max(100),  description: Joi.string().allow(null, ''),  first_receiver_role_uuid: Joi.string().uuid(),  final_approver_role_uuid: Joi.string().uuid(),}).min(1).messages({ 'object.min': 'At least one field is required' });