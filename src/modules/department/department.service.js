import departmentRepository from './department.repository.js';import ApiError from '../../utils/ApiError.js';class DepartmentService {  // Fetch all departments  async getAll() {    return departmentRepository.findAll();  }  // Fetch a single department by UUID — throws 404 if missing  async getByUuid(uuid) {    const department = await departmentRepository.findByUuid(uuid);    if (!department) {      throw ApiError.notFound('Department not found');    }    return department;  }  // Create a new department — checks code uniqueness  async create(data) {    const existing = await departmentRepository.findByCode(data.code);    if (existing) {      throw ApiError.conflict('Department code already exists');    }    return departmentRepository.create(data);  }  // Update a department by UUID — checks code uniqueness if changing  async update(uuid, data) {    const department = await departmentRepository.findByUuid(uuid);    if (!department) {      throw ApiError.notFound('Department not found');    }    if (data.code && data.code !== department.code) {      const existing = await departmentRepository.findByCode(data.code);      if (existing) {        throw ApiError.conflict('Department code already exists');      }    }    return department.update(data);  }  // Soft delete a department by UUID — throws 404 if missing  async delete(uuid) {    const deleted = await departmentRepository.delete(uuid);    if (!deleted) {      throw ApiError.notFound('Department not found');    }    return { message: 'Department deleted successfully' };  }}export default new DepartmentService();