import { Router } from 'express';import * as controller from './travel_local_transport.controller.js';import validate from '../../middleware/validate.js';import { createLocalTransportSchema, updateLocalTransportSchema } from './travel_local_transport.validation.js';const router = Router();router.get('/', controller.getAll);router.get('/:uuid', controller.getByUuid);router.post('/', validate(createLocalTransportSchema), controller.create);router.put('/:uuid', validate(updateLocalTransportSchema), controller.update);router.delete('/:uuid', controller.deleteRecord);export default router;